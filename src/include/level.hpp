#pragma once

#include <string>
#include <memory>

namespace level
{
    // добавить звуки в asset_manager
    
    void load(const std::string& name);

    //const sf::Vector2f& getSpawnPosition();
    //std::shared_ptr<...> getPlayer();
    //std::vector<std::shared_ptr<...>> getSomethingArray();
}